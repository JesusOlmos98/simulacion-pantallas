import { EnTextos } from '@/src/utils/common-lib-commac-generador/enumTextos';

// Map de textos en vietnamita - O(1) lookup
export const textMapVI = new Map<number | EnTextos, string>([[EnTextos.textNoText, '']]);
