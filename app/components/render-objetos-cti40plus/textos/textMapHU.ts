import { EnTextos } from '@/src/utils/common-lib-commac-generador/enumTextos';

// Map de textos en húngaro - O(1) lookup
export const textMapHU = new Map<number | EnTextos, string>([[EnTextos.textNoText, '']]);
