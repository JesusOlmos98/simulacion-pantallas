import { EnTextos } from '@/src/utils/common-lib-commac-generador/enumTextos';

// Map de textos en ruso - O(1) lookup
export const textMapRU = new Map<number | EnTextos, string>([[EnTextos.textNoText, '']]);
