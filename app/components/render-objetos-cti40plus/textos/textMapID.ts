import { EnTextos } from '@/src/utils/common-lib-commac-generador/enumTextos';

// Map de textos en indonesio - O(1) lookup
export const textMapID = new Map<number | EnTextos, string>([[EnTextos.textNoText, '']]);
