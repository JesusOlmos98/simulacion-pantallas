import { EnTextos } from '@/src/utils/common-lib-commac-generador/enumTextos';

// Map de textos en eslovaco - O(1) lookup
export const textMapSK = new Map<number | EnTextos, string>([[EnTextos.textNoText, '']]);
