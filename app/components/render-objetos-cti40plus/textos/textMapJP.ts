import { EnTextos } from '@/src/utils/common-lib-commac-generador/enumTextos';

// Map de textos en japonés - O(1) lookup
export const textMapJP = new Map<number | EnTextos, string>([[EnTextos.textNoText, '']]);
