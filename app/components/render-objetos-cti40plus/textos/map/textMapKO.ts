import { EnTextos } from '@/src/utils/common-lib-commac-generador/enumTextos';

// Map de textos en coreano - O(1) lookup
export const textMapKO = new Map<number | EnTextos, string>([[EnTextos.textNoText, '']]);
