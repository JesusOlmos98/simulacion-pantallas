import { EnTextos } from '@/src/utils/common-lib-commac-generador/enumTextos';

// Map de textos en turco - O(1) lookup
export const textMapTR = new Map<number | EnTextos, string>([[EnTextos.textNoText, '']]);
