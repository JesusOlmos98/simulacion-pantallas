import { EnTextos } from '@/src/utils/common-lib-commac-generador/enumTextos';

// Map de textos en polaco - O(1) lookup
export const textMapPL = new Map<number | EnTextos, string>([[EnTextos.textNoText, '']]);
