import { EnTextos } from '@/src/utils/common-lib-commac-generador/enumTextos';

// Map de textos en rumano - O(1) lookup
export const textMapRO = new Map<number | EnTextos, string>([[EnTextos.textNoText, '']]);
