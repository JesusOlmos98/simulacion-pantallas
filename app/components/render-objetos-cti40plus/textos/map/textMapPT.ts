import { EnTextos } from '@/src/utils/common-lib-commac-generador/enumTextos';

// Map de textos en portugués - O(1) lookup
export const textMapPT = new Map<number | EnTextos, string>([[EnTextos.textNoText, '']]);
