import { EnTextos } from '@/src/utils/common-lib-commac-generador/enumTextos';

// Map de textos en chino - O(1) lookup
export const textMapZH = new Map<number | EnTextos, string>([[EnTextos.textNoText, '']]);
