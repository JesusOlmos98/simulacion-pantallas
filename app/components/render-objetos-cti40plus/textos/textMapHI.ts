import { EnTextos } from '@/src/utils/common-lib-commac-generador/enumTextos';

// Map de textos en hindi - O(1) lookup
export const textMapHI = new Map<number | EnTextos, string>([[EnTextos.textNoText, '']]);
