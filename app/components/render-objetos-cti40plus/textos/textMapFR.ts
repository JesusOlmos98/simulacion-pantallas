import { EnTextos } from '@/src/utils/common-lib-commac-generador/enumTextos';

// Map de textos en francés - O(1) lookup
export const textMapFR = new Map<number | EnTextos, string>([[EnTextos.textNoText, '']]);
